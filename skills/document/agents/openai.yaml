interface:
  display_name: "Reqall Document"
  short_description: "Record one meaningful work item"
  default_prompt: "Use Reqall document to record this meaningful work item and link it to related memory."
  brand_color: "#4F46E5"
policy:
  allow_implicit_invocation: false
dependencies:
  tools:
    - type: "mcp"
      value: "reqall"
      description: "Reqall project memory tools"
      transport: "streamable_http"
      url: "https://www.reqall.net/mcp"
