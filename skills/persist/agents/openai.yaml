interface:
  display_name: "Reqall Persist"
  short_description: "Persist meaningful session outcomes"
  default_prompt: "Use Reqall persist to save this session's implementation, decisions, tests, and follow-ups."
  brand_color: "#4F46E5"
policy:
  allow_implicit_invocation: true
dependencies:
  tools:
    - type: "mcp"
      value: "reqall"
      description: "Reqall project memory tools"
      transport: "streamable_http"
      url: "https://www.reqall.net/mcp"
