interface:
  display_name: "Reqall Triage"
  short_description: "Classify and prioritize incoming work"
  default_prompt: "Use Reqall triage to classify this request, check for duplicates, and record the next action."
  brand_color: "#4F46E5"
policy:
  allow_implicit_invocation: true
dependencies:
  tools:
    - type: "mcp"
      value: "reqall"
      description: "Reqall project memory tools"
      transport: "streamable_http"
      url: "https://www.reqall.net/mcp"
